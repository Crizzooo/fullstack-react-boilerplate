/* Dependencies */


/* Initial State */
const initialState = {
};

/* Reducer Function */
export default (state = initialState, action) => {
  let newState = Object.assign({}, state);
  switch (action.type) {
    default:
        return newState;
  }
  return newState;
}
